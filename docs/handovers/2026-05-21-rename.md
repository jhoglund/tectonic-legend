# Handover — 2026-05-21 — `tectonic-for-the-win` → `tectonic-legend`

**Branch:** `main`
**Status:** in progress — code + GitHub rename done; **local directory rename pending** (the session that wrote this could not perform the rename itself because the Bash shell's CWD would be invalidated mid-process).

## What's done in the commit you're reading

- **In-repo string rename.** `package.json` (`name`), `vite.config.ts` (`base` + the four Mimir-proxy regex patterns), `index.html` (`<title>`), `capacitor.config.ts` (`appName` — not `appId`, see Open below), `specs/design-workflow.md` (Open Design absolute-path example), and the forward-looking docs (`docs/decisions/ADR-0006`, `docs/soft-launch-plan.md`, `docs/app-store-launch.md`).
- **In-app wordmark rename.** `HomeLanding.tsx`, `WelcomeScreen.tsx`, `SettingsScreen.tsx`, `RedeemCodeSheet.tsx`, `shareArtifact.ts` (the share-block header is now `Tectonic Legend —`).
- **GitHub repo renamed** to `jhoglund/tectonic-legend`. Local remote URL updated. GitHub auto-redirects every old repo URL forever.
- **Pushed.** Pages auto-deploys at the new URL `https://jhoglund.github.io/tectonic-legend/`.
- **Verified.** Lint clean, 84 tests pass, build green under the new name.

## What's pending — for Jonas, outside any Claude session

```bash
mv ~/dev/tectonic-for-the-win ~/dev/tectonic-legend
```

That's the directory rename. **Do this between sessions** — running it from inside Claude Code's shell breaks the shell's CWD.

After the move:

1. Close every editor / Finder / terminal window pointed at the old path.
2. Kill any running dev server (port 7576 will be held by the previous Vite process; `lsof -ti tcp:7576 | xargs kill`).
3. Open a fresh Claude Code session in `~/dev/tectonic-legend/`.

## First steps in the new session

1. `pwd` ends in `tectonic-legend` ✓.
2. `npm install` — regenerates `package-lock.json` under the new `name`. (Optional: `git add package-lock.json && git commit -m "deps: refresh lockfile after rename"` if it changes.)
3. `npm run dev` — confirm Vite starts on 7576 and the dev server is reachable at `https://tectonic.test/` (puma-dev hostname is **unchanged**; see Open below).
4. Smoke the rename worked end-to-end: open Stats, flip developer mode on, set Mastery → `legend` from DevTools, confirm the chip + halo + progress bar render and the Home stage chip reads `Legend ✦`.
5. `git log --oneline origin/main` — the last commit should be the rename PR. Above it: ADR-0018 implementation (`cc5d95c`), the spec layer (`a4d8de1`), the brand-name ADR (`bd6ad27`).

## Context the new session needs (and won't find in CLAUDE.md or the ADRs)

- **The brand is `Tectonic Legend`** (ADR-0006, accepted 2026-05-20). Treat it as final — trademark filing + domain acquisition haven't happened but the call has been made.
- **The Capacitor `appId` is deliberately still `com.jhoglund.tectonic`.** Not an oversight. iOS bundle IDs are permanent once the App Store Connect record exists, so the decision to align it to `com.jhoglund.tectoniclegend` is held for just-before-submission. Don't change it without an explicit ask.
- **puma-dev hostname is decoupled from the repo name.** `~/.puma-dev/tectonic` is named after the *hostname* `tectonic.test`, not the repo — it survived the rename and `vite.config.ts allowedHosts: ['tectonic.test']` still works. Renaming the hostname to `tectonic-legend.test` is an *optional* future task; it costs a `mv ~/.puma-dev/tectonic ~/.puma-dev/tectonic-legend` + one line in `vite.config.ts` + a `puma-dev -stop` / restart. Holding off keeps the local URL Jonas has muscle-memory for.
- **Dev menu access is automatic on `npm run dev`.** `useEffectiveDeveloper` returns true under `import.meta.env.DEV` (see ADR-0017's developer-access section). Production builds still require the 7-tap unlock or an allowlisted email — that path is intact.
- **Auth is anonymous-by-default** (ADR-0017). `AuthProvider` silently calls `signInAnonymously()` on first launch when no session exists. The Save-your-progress sheet only surfaces from Settings (and, later, value moments).
- **The Supabase dashboard is not configured yet** for the new auth flow. End-to-end verification is blocked on enabling Anonymous sign-ins, Apple, Google providers and SMTP — out-of-band work for Jonas. ADR-0017's Consequences section lists the steps; nothing in code changes when he does them.
- **The Legend stage + depth-score implementation shipped today** (`cc5d95c`, ADR-0018). Test it via DevTools' AUTH+MASTERY groups — `legend` fixture seeds a synthetic solve history so the depth-score's QUALITY/DIFFICULTY terms peg.
- **The Vite dev server (`tectonic` config in `.claude/launch.json`) and the Preview MCP cache absolute paths.** A new session in the new directory should treat any cached `serverId` as invalid and start fresh.
- **The two share-link URLs Jonas pasted earlier in the project** (`https://jhoglund.github.io/tectonic-for-the-win/#…`) won't load directly anymore — the Pages site moved. GitHub keeps a redirect, so they likely still work; if they don't, the puzzle data is in the URL hash and can be reshared under the new host.

## Open items / decisions queued

| Item | What | Status |
|---|---|---|
| **A.** Capacitor `appId` | `com.jhoglund.tectonic` → `com.jhoglund.tectoniclegend` | Held; decide just before creating the App Store record |
| **B.** puma-dev hostname | `tectonic.test` → `tectonic-legend.test` | Optional; held for now |
| **C.** ADR-0019 implementation | Apprentice → Mythic Legend tiers + daily-puzzle leaderboard | Spec-only; landed in `a4d8de1`. Engine + Supabase work pending |
| **D.** Auth end-to-end verification | Supabase dashboard providers + a real device sign-in | Blocked on Jonas's dashboard work |
| **E.** Pair-elimination chip | The ADR-0016 follow-up — extend the value-set chip to the pair-elimination cause region | Flagged in ADR-0016's "Implies" |
| **F.** Tutorial polish | Item 10 in `docs/backlog.md` — flagged as needing pacing + visual treatment before soft launch | Known v1 debt |
| **G.** Replace stock Vite `README.md` | App-store launch audit R7 lists this as P1 hygiene | Not started |

## Files touched in this commit

- `package.json` — `name`.
- `vite.config.ts` — `base`, four proxy regex patterns.
- `index.html` — `<title>`.
- `capacitor.config.ts` — `appName` (kept `appId`, see Open A).
- `specs/design-workflow.md` — Open Design example path.
- `docs/decisions/ADR-0006-app-name.md` — placeholder language + follow-up status.
- `docs/soft-launch-plan.md`, `docs/app-store-launch.md` — old name → new; R7 audit row resolved.
- `src/screens/HomeLanding.tsx`, `WelcomeScreen.tsx`, `SettingsScreen.tsx` — in-app wordmark.
- `src/components/RedeemCodeSheet.tsx` — `Tectonic Legend Premium`.
- `src/lib/shareArtifact.ts` — share-block header.

## Lifecycle

Delete this file once the new session has run `npm install`, confirmed the dev server works, and the smoke test passes. Items A–G then live where they belong — A, B in this file's mention until resolved (move to `docs/backlog.md`); C–G already on the backlog.
